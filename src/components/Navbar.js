import React from 'react';
import styles from './Navbar.module.css';

function Navbar() {
  return (
    <nav className={styles.navbar}>
      {/* Navbar content can be added here if needed */}
    </nav>
  );
}

export default Navbar;
